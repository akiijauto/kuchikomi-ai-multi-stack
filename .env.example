# docker compose がビルド引数として読む公開値（秘密情報ではない）。
# このファイルを .env にコピーして値を入れる。サーバー側の鍵は web/.env に置く。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_OPERATOR_NAME=
NEXT_PUBLIC_OPERATOR_EMAIL=
